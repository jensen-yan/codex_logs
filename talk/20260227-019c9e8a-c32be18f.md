# Conversation 019c9e8a

- Date: 2026-02-27
- Started: 2026-02-27 17:59 CST
- Session: `019c9e8a-0c57-7ee0-b42b-37f1c32be18f`
- Source: `/nfs/home/yanyue/.codex/sessions/2026/02/27/rollout-2026-02-27T17-59-33-019c9e8a-0c57-7ee0-b42b-37f1c32be18f.jsonl`
- Messages: 6
- Roles: `all`
- Exported: 2026-03-16 14:56 CST

---

## User (17:59)

请阅读 /nfs/home/yanyue/.codex/skills/.system/skill-creator/SKILL.md，提炼在本仓库创建新skill时必须包含的最小结构、文档字段、脚本组织方式与验证步骤。给出精简清单。

## Assistant (17:59)

我会按 `skill-creator` 的规范来提炼清单。先读取你指定的 `SKILL.md`，抓取“最小必需项”（结构、字段、脚本组织、验证流程），再给出精简版检查表。

## Assistant (17:59)

[Tool: exec_command]

```bash
wc -l /nfs/home/yanyue/.codex/skills/.system/skill-creator/SKILL.md && sed -n '1,220p' /nfs/home/yanyue/.codex/skills/.system/skill-creator/SKILL.md
```

## Assistant (17:59)

[Tool: exec_command]

```bash
sed -n '221,500p' /nfs/home/yanyue/.codex/skills/.system/skill-creator/SKILL.md
```

## Assistant (17:59)

我已读完 `skill-creator` 文档并抽取了“必须项”和“最小可行项”。下一条给你一个可直接照抄的精简检查清单，按“结构/字段/脚本/验证”分组。

## Assistant (18:00)

基于 [SKILL.md](/nfs/home/yanyue/.codex/skills/.system/skill-creator/SKILL.md) 提炼的**新建 skill 最小清单**如下。

**背景**
- 目标是“最小可用且可触发”的 skill：先保证触发准确、结构合法、可验证通过，再逐步加资源。

**1. 最小结构（必须）**
- `skill-name/`
- `skill-name/SKILL.md`（唯一硬性必需文件）
- 目录名必须与 skill 名一致（`lowercase + digits + hyphens`，建议 <64 字符）

**2. 文档字段（SKILL.md 必须）**
- YAML frontmatter 只允许两个字段：
- `name: <skill-name>`
- `description: <触发描述，含“做什么 + 何时使用”>`
- frontmatter 后必须有 Markdown 正文（操作说明）
- 不要在 frontmatter 加其他键

**3. 脚本组织（最小原则）**
- 仅在“重复改写代码”或“需要确定性执行”时才建 `scripts/`
- `scripts/` 内放可执行脚本（Python/Bash 等），优先承载易重复流程
- 如果用了模板初始化的示例脚本，保留有用的，删除占位文件
- 新增脚本必须实际运行测试（至少代表性样本）

**4. 验证步骤（必须执行）**
1. 运行结构/字段校验：
```bash
scripts/quick_validate.py <path/to/skill-folder>
```
2. 若有 `scripts/`，逐个或抽样实际执行，确认输出正确
3. 修复后再次运行 `quick_validate.py`，直到通过

**风险**
- `description` 写得泛或漏触发场景，会导致 skill 不触发或误触发
- frontmatter 多写字段会被校验拦下
- 留下示例占位文件会污染实际使用
- 脚本不跑实测，后续调用时容易失败

**改进建议（在最小清单之上）**
- 建议补 `agents/openai.yaml`（推荐项，不是硬性必需）
- `SKILL.md` 保持精简，详资料放 `references/`，避免正文膨胀
- 长文档加目录，且从 `SKILL.md` 直接一跳可达（避免深层引用）
